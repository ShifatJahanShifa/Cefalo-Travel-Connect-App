import { transportDTO } from "../../src/DTOs/transport.dto.ts";

export const mockTransportDTO: transportDTO = {
  transport_id: 't001',
  transport_type: 'Bus',
  transport_name: 'Hanif Poribohon',
};
